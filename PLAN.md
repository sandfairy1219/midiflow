# 개발 계획서: AI 음악 생성 + 편집 통합 프로그램 (가칭 "MidiFlow")

> Suno의 완성도 방향 + DAW의 세부 편집을 하나의 프로그램에서.
> 오픈소스 모델을 조립하고, 생성 → 편집 → 재생성 루프를 제공한다.

---

## 1. 목표

| 항목 | 내용 |
|---|---|
| 핵심 목표 | 텍스트/레퍼런스로 음악을 생성하고, 결과물을 노트 단위로 수정한 뒤 다시 AI로 완성도 있게 렌더링 |
| 사용자 | 작곡 지식이 없는 사용자 (생성은 AI에 위임, 편집은 직관적 UI로) |
| 결과물 | 완성된 오디오 파일 (wav/mp3) + 편집 가능한 프로젝트 파일 |

---

## 2. 핵심 컨셉: 생성 → 편집 → 재생성 루프

Suno가 못 해주는 "멜로디 유지하며 스타일만 변경"을 이 루프로 구현한다.

```
[텍스트 프롬프트 or 레퍼런스 곡]
        │
        ▼
  ① AI 생성 (MusicGen / YuE)
        │
        ▼
  ② 분석 (스템 분리 + 오디오→MIDI 추출)
        │
        ▼
  ③ 편집 (피아노롤에서 노트/리듬 수정)   ←── 사용자 개입 지점
        │
        ▼
  ④ 재생성 (편집된 MIDI를 멜로디 조건으로 AI 재렌더링)
        │
        ▼
  ⑤ 믹싱/마스터링 → 최종 오디오 내보내기
```

- ③↔④는 무한 반복 가능 (코드 수정 → 빌드처럼, 편집 → 재생성)
- 멜로디는 MIDI로 고정하므로 Suno처럼 "전혀 다른 곡"이 되지 않음

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────┐
│  Frontend (Web / React)                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ 프롬프트  │ │ 피아노롤  │ │ 믹서(볼륨/팬) │ │
│  │  입력 UI │ │ 에디터   │ │  + 트랙 뷰   │ │
│  └──────────┘ └──────────┘ └──────────────┘ │
└──────────────────┬──────────────────────────┘
                   │ REST / WebSocket
┌──────────────────┴──────────────────────────┐
│  Backend (Python / FastAPI)                 │
│  ┌───────────────────────────────────────┐  │
│  │ Pipeline Orchestrator                 │  │
│  │  - 생성 요청 관리 (작업 큐)            │  │
│  │  - 프로젝트 상태 관리                  │  │
│  └───────────────────────────────────────┘  │
│  ┌─────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ 생성     │ │ 분석      │ │ 렌더링/믹싱  │ │
│  │ MusicGen │ │ Demucs   │ │ pedalboard   │ │
│  │ YuE      │ │ BasicPitch│ │ pydub       │ │
│  └─────────┘ └──────────┘ └──────────────┘ │
└──────────────────┬──────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │  GPU (RTX 3060 12GB) │
        │  PyTorch + CUDA      │
        └─────────────────────┘
```

---

## 4. 오픈소스 모델 선택 (역할별)

| 역할 | 모델 | 이유 | VRAM (12GB 기준) |
|---|---|---|---|
| 텍스트→음악 (기본) | **MusicGen medium** (Meta/audiocraft) | 안정적, 로컬 실행 쉬움 | ✅ 충분 |
| 멜로디 조건 재생성 | **MusicGen melody conditioning** | 편집된 MIDI를 따라가며 재생성 (핵심 기능) | ✅ 충분 |
| 전체 곡 + 보컬 (선택) | **YuE** | Suno형 전체곡 생성, 양자화 필요 | ⚠️ 빠듯 (양자화 필수) |
| 인스트루멘털 고품질 | **Stable Audio Open** | 텍스처/반주 생성 보조 | ✅ 충분 |
| 스템 분리 | **Demucs v4** | 보컬/드럼/베이스/기타 분리 (사실상 표준) | ✅ 충분 |
| 오디오→MIDI | **Basic Pitch** (Spotify) | 멜로디 추출, 가벼움 | ✅ CPU도 가능 |

> 1차 MVP는 MusicGen + Demucs + Basic Pitch 3개만. YuE는 Phase 후반에 추가.

---

## 5. 기술 스택

### Backend
- Python 3.11, **FastAPI** (API) + **uvicorn**
- PyTorch (CUDA), **audiocraft**, **demucs**, **basic-pitch**
- MIDI 처리: **pretty_midi**, mido
- 오디오 처리: librosa, soundfile, **pedalboard** (이펙트)
- 작업 큐: asyncio 기반 인메모리 큐 (MVP) → 필요시 Celery

### Frontend
- **React + Vite + TypeScript**
- 재생/프리뷰: **Tone.js** (MIDI 프리뷰 재생)
- 파형 표시: wavesurfer.js
- 피아노롤: Canvas 기반 자체 구현 (또는 webaudio-pianoroll 라이브러리 활용)
- 패키징(선택): Tauri 또는 Electron (데스크톱 앱화)

### 데이터
- 프로젝트 파일: JSON (트랙/노트/설정) + 오디오/MIDI 파일은 폴더로 관리
- DB 없이 파일 시스템 기반 (MVP)

---

## 6. 기능 명세

### MVP (최소 기능)
1. 텍스트 프롬프트로 음악 생성 (MusicGen)
2. 생성 결과를 스템 분리 + MIDI 추출
3. 피아노롤에서 노트 보기/편집 (이동, 길이, 삭제, 추가)
4. 트랙별 볼륨/뮤트 + Tone.js 프리뷰 재생
5. 편집된 MIDI로 재생성 (melody conditioning)
6. 최종 오디오 내보내기 (wav)

### 확장 (2차)
- 구간 재생성 (chorus만 다시 만들기)
- 레퍼런스 곡 업로드 → 멜로디 추출 → 스타일 변경
- 간단 믹싱 (EQ/리버브/컴프레서, pedalboard 프리셋)
- YuE 통합 (보컬 포함 전체곡)
- 데스크톱 앱 패키징

---

## 7. 개발 단계

### Phase 0 — 환경 구축 (0.5~1일)
- Python venv, PyTorch CUDA 설치 (RTX 3060 확인 완료)
- MusicGen medium 다운로드 + 샘플 생성 테스트
- 목표: CLI에서 `generate "lofi beat" → out.wav` 동작

### Phase 1 — 생성 파이프라인 (1~2일)
- FastAPI 서버: `POST /generate` (프롬프트 → 오디오 파일)
- 작업 큐 + 진행 상태 폴링
- 산출물: API로 생성/파일 조회 가능

### Phase 2 — 분석 파이프라인 (1~2일)
- `POST /analyze`: 오디오 → Demucs 스템 분리 → Basic Pitch로 MIDI 추출
- 산출물: 트랙별 MIDI 파일 + 스템 오디오

### Phase 3 — 웹 UI 기초 (2~3일)
- React 앱: 프롬프트 입력, 생성 요청, 오디오 재생(파형 표시)
- 트랙 리스트 + 볼륨/뮤트

### Phase 4 — 피아노롤 에디터 (3~5일) ← 가장 큰 공수
- MIDI를 피아노롤로 렌더링 (Canvas)
- 노트 드래그 이동, 길이 조절, 삭제, 추가
- Tone.js로 편집 중 프리뷰 재생
- 산출물: 편집 결과를 MIDI로 저장

### Phase 5 — 재생성 루프 (1~2일)
- 편집 MIDI → 멜로디 조건 입력으로 변환 → MusicGen 재생성
- "스타일만 다시" 프롬프트 변경 옵션
- 산출물: 핵심 루프 완성 (③→④)

### Phase 6 — 믹싱/내보내기 (1~2일)
- 트랙별 볼륨/팬, 마스터에 간단 이펙트 체인
- wav/mp3 내보내기

### Phase 7 — 확장 (선택)
- YuE, 구간 재생성, 레퍼런스 스타일 이식, 데스크톱 패키징

**총 예상: MVP(Phase 0~6) ≈ 10~17일** (AI 페어 프로그래밍 기준)

---

## 8. 디렉토리 구조 (제안)

```
midi/
├── PLAN.md                 ← 이 문서
├── backend/
│   ├── main.py             # FastAPI 엔트리
│   ├── pipelines/
│   │   ├── generate.py     # MusicGen 생성
│   │   ├── analyze.py      # Demucs + Basic Pitch
│   │   ├── regenerate.py   # 멜로디 조건 재생성
│   │   └── render.py       # 믹싱/내보내기
│   ├── models.py           # 데이터 스키마 (프로젝트/트랙/노트)
│   └── projects/           # 프로젝트 데이터 (생성물)
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── PianoRoll.tsx
│   │   │   ├── TrackList.tsx
│   │   │   └── PromptPanel.tsx
│   │   ├── api.ts
│   │   └── App.tsx
│   └── package.json
└── requirements.txt
```

---

## 9. 하드웨어 / 한계 (솔직한 평가)

- **RTX 3060 12GB**: MusicGen medium/large, Demucs, Basic Pitch, Stable Audio Open 모두 가능. YuE는 양자화로만 가능하며 품질 저하 있음.
- **완성도 한계**: 로컬 오픈소스 결과물은 Suno보다 아래. 특히 보컬/실사 연주 느낌. 이 프로그램의 강점은 "완성도"가 아니라 **"편집 가능성 + 멜로디 고정 재생성"**이다.
- **MIDI 추출 정확도**: Basic Pitch는 단선율은 정확하지만 복잡한 화음/드럼은 오류 있음 → 편집 단계에서 교정하는 구조로 커버.
- **재생성 품질**: 멜로디 조건 생성도 완벽히 멜로디를 따르지는 않음 (90% 수준). 반복 생성으로 선택.

---

## 10. 성공 기준

1. "We Wish You a Merry Christmas" 멜로디 MIDI를 넣고, "rock" 프롬프트로 재생성 시 **멜로디가 유지된 채 스타일만 바뀐 결과물**이 나올 것
2. 프롬프트 → 생성 → 편집 → 재생성 → 내보내기 전체 루프가 프로그램 하나에서 끝날 것
